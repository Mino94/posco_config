my-config.name: defualt update

management:
    endpoints:
        web:
            exposure:
                include: refresh
    endpoint:
        shutdown:
            enabled: true
    security:
        enabled: false
