my-config.name: defualt update zzzzzzzz

spring:
    rabbitmq:
        host: localhost
        port: 5672
        username: guest
        password: guest

management:
    endpoints:
        web:
            exposure:
                include: refresh, busrefresh
    endpoint:
        shutdown:
            enabled: true
    security:
        enabled: false
